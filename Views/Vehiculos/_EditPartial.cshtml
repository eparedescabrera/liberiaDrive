@model LiberiaDriveMVC.Models.Vehiculo

<form id="formEditarVehiculo">
    <input type="hidden" asp-for="IdVehiculo" />

    <div class="mb-3">
        <label class="form-label">Marca</label>
        <input asp-for="Marca" class="form-control" placeholder="Ej: Toyota" />
        <span asp-validation-for="Marca" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label class="form-label">Modelo</label>
        <input asp-for="Modelo" class="form-control" placeholder="Ej: Corolla" />
    </div>

    <div class="mb-3">
        <label class="form-label">Transmisión</label>
        <select asp-for="Transmision" class="form-select">
            <option value="Manual" selected="@Model.Transmision == "Manual"">Manual</option>
            <option value="Automática" selected="@Model.Transmision == "Automática"">Automática</option>
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label">Placa</label>
        <input asp-for="Placa" class="form-control" placeholder="Ej: ABC123" />
    </div>

    <div class="mb-3">
        <label class="form-label">Estado</label>
        <select asp-for="Estado" class="form-select">
            <option value="Disponible" selected="@Model.Estado == "Disponible"">Disponible</option>
            <option value="En uso" selected="@Model.Estado == "En uso"">En uso</option>
            <option value="En mantenimiento" selected="@Model.Estado == "En mantenimiento"">En mantenimiento</option>
        </select>
    </div>

    <div class="text-end">
        <button type="submit" class="btn btn-warning">
            <i class="fa fa-save"></i> Actualizar
        </button>
    </div>
</form>
