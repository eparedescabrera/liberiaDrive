@{
    
     var idPago = ViewBag.IdPago ?? 0;
}

<form id="formEliminarPago">
    <input type="hidden" name="id" value="@idPago" />

    <div class="text-center p-3">
        <i class="bi bi-exclamation-triangle text-danger" style="font-size: 3rem;"></i>
        <h4 class="mt-2 fw-bold text-danger">¿Desea eliminar este pago?</h4>
        <p class="text-muted">
            Esta acción no se puede deshacer. El registro del pago será eliminado de forma permanente.
        </p>
    </div>

    <div class="text-end mt-4">
        <button type="submit" class="btn btn-danger">🗑️ Eliminar</button>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">❌ Cancelar</button>
    </div>
</form>
