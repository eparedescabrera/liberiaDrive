<!-- =========================================================
 🧾 Modal genérico reutilizable para Inscripción de Curso
========================================================= -->
<div class="modal fade" id="modalInscripcion" tabindex="-1" aria-labelledby="tituloModal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content shadow border-0">
            <div class="modal-header bg-dark text-white">
                <h5 class="modal-title" id="tituloModal">Gestión de Inscripción</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>

            <div class="modal-body p-0" id="contenidoModal">
                <!-- Aquí se cargan dinámicamente las vistas parciales (_CreatePartial, _EditPartial, _DeletePartial) -->
                <div class="text-center p-4 text-muted">
                    <i class="bi bi-arrow-repeat display-6 text-secondary"></i>
                    <p class="mt-3 mb-0">Cargando contenido...</p>
                </div>
            </div>
        </div>
    </div>
</div>
